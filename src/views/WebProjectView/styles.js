import emotion from '@emotion/styled';
import flex from 'styles/flex';
import { css } from 'emotion';

export const WebProjectView = emotion.div({
  ...flex.vertical,
  flex: 1
});

export const Webview = css({
  flex: 1
});
