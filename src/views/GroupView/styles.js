import emotion from 'react-emotion';
import flex from 'styles/flex';

export const GroupView = emotion.div({
  ...flex.vertical,
  color: 'white',
  flex: 1
});

export const Name = emotion.div({});

export const Projects = emotion.div({});
