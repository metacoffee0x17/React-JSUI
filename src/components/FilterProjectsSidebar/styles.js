import emotion from '@emotion/styled';
import flex from 'styles/flex';

export const FilterProjectsSidebar = emotion.div({
  ...flex.vertical
});

export const Title = emotion.div({
  fontSize: 15
});
