const addEnv = require('./add-env');
const addPlop = require('./add-plop');
const rewire = require('./rewire');
const storybook = require('./storybook');
const customReactScripts = require('./custom-react-scripts');

module.exports = {
  addEnv,
  addPlop,
  rewire,
  storybook,
  customReactScripts
};
